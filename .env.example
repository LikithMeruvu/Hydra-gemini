# ── Redis ─────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Gateway Server ───────────────────────
TUI_HOST=127.0.0.1
TUI_PORT=8000

# ── Router Tuning ────────────────────────
TUI_HEALTH_WEIGHT=0.4
TUI_CAPACITY_WEIGHT=0.6
TUI_RETRY_ATTEMPTS=3
TUI_FALLBACK_ENABLED=true

# ── Logging ──────────────────────────────
TUI_LOG_LEVEL=INFO

# ── Global Exposure ────────────────────────
# TUI_EXPOSE_TOKEN=...  # (Optional) Cloudflare Tunnel token if manually managed

